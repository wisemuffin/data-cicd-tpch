version: 2

models:

    - name: orders_items
      tests:
          - dbt_utils.equality:
              compare_model: ref('orders_items_expected') 
      columns:
          - name: order_line_number
            tags:
              - pk_tag